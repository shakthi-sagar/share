{
  "name": "encrypted-share",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "typescript": "7.0.2"
  }
}
